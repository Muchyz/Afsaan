export default function Tag({ children }) {
  return (
    <span className="inline-block bg-offwhite border border-sky/30 text-navy text-xs font-semibold px-2.5 py-1 rounded-full mr-1.5 mb-1.5">
      {children}
    </span>
  )
}
